# Daily Mattr — environment

# Public (safe to bundle): Supabase project the app talks to.
VITE_SUPABASE_URL=https://ygxdrphajvrbjcaxhvcn.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Subscribe (anonymous) endpoint override (defaults baked into src/lib/subscribeApi.js)
# VITE_SUBSCRIBE_URL=
# VITE_NEWS_BASE_URL=

# Server-side only (ingest script) — never prefix VITE_
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small
CLUSTER_THRESHOLD=0.62
MAX_AGE_HOURS=30
